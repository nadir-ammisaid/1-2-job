# ============================================
# Railway Database Configuration
# ============================================
# Get these values from Railway:
# Railway → MySQL service → Variables tab
# Copy from MYSQL_PUBLIC_URL and decompose it

DB_HOST=turntable.proxy.rlwy.net
DB_PORT=15337
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=railway

# ============================================
# JWT Secret
# ============================================
# Generate a secure random string
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

JWT_SECRET=your_jwt_secret_here

# ============================================
# Environment
# ============================================

NODE_ENV=production

# ============================================
# Admin Creation (Optional - Only for create-admin.js script)
# ============================================
# These are used ONLY when running the create-admin script
# After creating the admin, you should DELETE or COMMENT these lines
# for security purposes

ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=SuperUser
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeThisSecurePassword123!

# ============================================
# Instructions
# ============================================
# 1. Copy this file to .env.railway
# 2. Fill in the actual values from Railway
# 3. Run: node scripts/create-admin.js
# 4. After admin creation, delete the ADMIN_* variables
# 5. NEVER commit .env.railway to git!